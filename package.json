{
  "name": "gz-school-research",
  "private": true,
  "version": "0.1.0",
  "description": "广州学校升学调研：Web (Vue3) + 微信小程序 (原生) + 共享核心包 (@gz/shared)",
  "workspaces": [
    "packages/*",
    "apps/web"
  ],
  "scripts": {
    "build:shared": "npm run build -w @gz/shared",
    "dev:web": "npm run dev -w @gz/web",
    "build:web": "npm run build -w @gz/web",
    "preview:web": "npm run preview -w @gz/web",
    "build:mp": "node scripts/miniprogram/build.mjs",
    "test": "npm test -w @gz/shared",
    "check": "node scripts/check_step.mjs 'typecheck' 'npm run typecheck --workspaces --if-present' && node scripts/check_step.mjs 'shared 单测' 'npm test -w @gz/shared' && python3 scripts/data_quality_test.py && python3 data/poi/test/test_match_poi.py && python3 data/registry/group/test/check_groups_drift.py && python3 data/primary/transition/test/check_middle_feed_snapshot.py && python3 data/primary/enrollment/test/check_enrollment_snapshot.py && python3 data/middle/enrollment/test/check_middle_snapshot.py && python3 data/awards/test/test_awards_build.py && python3 data/civilized_campuses/test/test_build_dist.py && node scripts/data/compact.mjs >/dev/null && echo \"✓ compact 产物已刷新（不入库，check 顺带重编译）\""
  }
}
